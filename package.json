{
  "name": "cachepromise",
  "version": "1.0.0",
  "description": "A simple in-memory cache with TTL support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "jest --config jest.config.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "cache",
    "memory-cache",
    "ttl",
    "typescript"
  ],
  "author": "Merkie",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^18.19.64",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
